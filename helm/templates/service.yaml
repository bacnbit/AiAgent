apiVersion: v1
kind: Service
metadata:
  name: {{ include "aiagent.fullname" . }}
  labels:
    {{- include "aiagent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aiagent.selectorLabels" . | nindent 4 }}
